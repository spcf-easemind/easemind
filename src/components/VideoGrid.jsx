import { Grid, AspectRatio } from "@mantine/core";

export default function VideoGrid() {
  return (
    <Grid>
      <Grid.Col span={6}>
        <AspectRatio>
          {/* Video Component */}
        </AspectRatio>
      </Grid.Col>
    </Grid>
  );
}
